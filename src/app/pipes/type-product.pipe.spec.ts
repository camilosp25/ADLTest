import { TypeProductPipe } from './type-product.pipe';

describe('TypeProductPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeProductPipe();
    expect(pipe).toBeTruthy();
  });
});
