import { CreditcardiconPipe } from './creditcardicon.pipe';

describe('CreditcardiconPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditcardiconPipe();
    expect(pipe).toBeTruthy();
  });
});
